import { Lightbulb, ScanSearch, Network, CodeXml, Activity, Check } from "lucide-react";
import { Section, SectionHeading } from "./Section";
const steps=[[Lightbulb,"Descoberta","Entender o problema e as necessidades."],[ScanSearch,"Análise","Mapear processos, requisitos e oportunidades."],[Network,"Arquitetura","Definir a estrutura técnica da solução."],[CodeXml,"Desenvolvimento","Construir, testar e validar a solução."],[Activity,"Evolução","Monitorar resultados e melhorar continuamente."]] as const;
export function Workflow(){return <Section id="processo" className="section-band"><SectionHeading eyebrow="06 / PROCESSO" title="Como transformo uma ideia em solução" center/><div className="workflow">{steps.map(([Icon,title,text],i)=><article className="workflow-step" key={title}><div className="workflow-icon"><Icon/></div><span>0{i+1}</span><h3>{title}</h3><p>{text}</p>{i<steps.length-1&&<div className="workflow-line"/>}</article>)}</div><div className="manifesto"><div><span className="section-eyebrow">O DIFERENCIAL</span><h2>Mais do que tecnologia.<br/><span className="text-gradient">Soluções que fazem sentido.</span></h2></div><div className="grid gap-5">{[["Visão analítica","Entender antes de desenvolver."],["Foco em resultados","Utilizar tecnologia para resolver problemas reais."],["Melhoria contínua","Buscar evolução constante dos sistemas e processos."]].map(([t,d])=><div className="pillar" key={t}><Check/><div><strong>{t}</strong><p>{d}</p></div></div>)}</div></div></Section>}
