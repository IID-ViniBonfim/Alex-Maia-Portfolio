import { Dialog, DialogContent, DialogDescription, DialogHeader, DialogTitle } from "@/components/ui/dialog";
import type { Project } from "./data";
export function ProjectModal({ project, onClose }: { project: Project | null; onClose: () => void }) { return <Dialog open={Boolean(project)} onOpenChange={open => !open && onClose()}>{project && <DialogContent className="max-h-[88vh] max-w-2xl overflow-y-auto border-border bg-card p-0"><div className={`project-art project-art-${project.id} min-h-44`}><span className="project-index">PROJECT / {project.id}</span><div className="project-screen"><i/><i/><i/></div></div><div className="p-6 sm:p-8"><DialogHeader><DialogTitle className="font-display text-2xl">{project.name}</DialogTitle><DialogDescription className="leading-6">Estudo de caso fictício para demonstração de portfólio.</DialogDescription></DialogHeader><div className="mt-7 grid gap-6 sm:grid-cols-2">{[["Problema",project.problem],["Objetivo",project.objective],["Solução",project.solution],["Resultados",project.results]].map(([label,text])=><div key={label}><h4 className="mb-2 text-xs font-semibold uppercase tracking-[.14em] text-primary">{label}</h4><p className="text-sm leading-6 text-muted-foreground">{text}</p></div>)}</div><div className="mt-7 flex flex-wrap gap-2">{project.tech.map(t=><span className="tech-tag" key={t}>{t}</span>)}</div></div></DialogContent>}</Dialog> }
